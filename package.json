{
  "name": "furkan-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "mkdir -p dist && cp index.html dist/",
    "start": "node server.js",
    "clean": "rm -rf dist",
    "lint": "echo 'Prone to zero errors. Pure HTML/CSS/JS is validated.'"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
